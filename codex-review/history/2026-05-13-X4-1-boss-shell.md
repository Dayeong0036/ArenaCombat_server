# X4-1: BossNetworkController3D ICombatant Compile-Bridge Shell — 2026-05-13

**Status**: APPLIED. Codex Round 1 APPROVED WITH CHANGES (2 critical-ish + 3 suggestion, all applied).

## Scope

1 NEW file. PNC3D X3-1 패턴 그대로 미러 (compile-bridge only, no manager wiring, no FSM, no ML).

## Files

- NEW `Assets/ArenaCombat/Scripts/Core/Network/BossNetworkController3D.cs` (~110 LOC)
- NEW `Assets/ArenaCombat/Scripts/Core/Network/BossNetworkController3D.cs.meta` (GUID `0bc3625afac9487db2939f933bd06f27`)

## Codex Critical Applied

- **C-1 `_bossStatsSO` SerializeField 제거**: pending.md에서 placeholder 포함 여부를 Codex 콜로 두었고, Codex가 "compile-only shell 범위를 흐린다" 판단 → X4-2에서 StatManager.Initialize 실제 wiring 시 추가하기로. 현재 파일에 포함 없음.
- **C-2 inert defaults 안전화**: `IsAlive=true`/`CurrentHPPercent=1` → `IsAlive=false`/`CurrentHPPercent=0`/`MaxHP=0`. 실수로 씬/registry/skill target path에 들어가도 "살아있는 보스"로 취급되지 않음. X4-2에서 StatManager route 붙으면 실제 값으로 교체.

## Codex Suggestions Applied

- **S-1 `_x4StubWarned`는 static readonly**: instance별 1회보다 process-wide 1회가 로그 소음 ↓. X3-1 `_x3StubWarned` 패턴과 동일.
- **S-2 `[DisallowMultipleComponent]`**: 같은 GameObject에 여러 개 붙을 이유 없음.
- **S-3 헤더 명시**: "X4-1 shell only. Do not place in production scene / do not register as NetworkPrefab until X4 spawn path lands." 추가.

## Surface

```csharp
[DisallowMultipleComponent]
[RequireComponent(typeof(NetworkObject))]
public class BossNetworkController3D : NetworkBehaviour, ICombatant
{
    private static readonly HashSet<string> _x4StubWarned = new HashSet<string>();
    private void WarnX4Stub(string memberName) { /* warn-once Debug.LogWarning */ }

    // 9 property impls (explicit) — inert defaults: false / 0 / 0 / 0 / false / false / 0
    // 14 method impls (explicit) — WarnX4Stub + no-op (HasStatus returns false)
}
```

## Verification

1. Unity recompile: 1 new file, expect <3s. 0 new errors / 0 new warnings.
2. `Add Component > BossNetworkController3D` Inspector에 노출.
3. NetworkPrefabs 미등록 → 런타임 스폰 경로 비활성. X3 smoke test와 완전 분리.
4. ICombatant 23 멤버 (9 prop + 14 method) explicit impl 모두 grep 가능.

## Risks (Acknowledged)

- **NetworkPrefab 미등록**: 의도된 상태. X4-5 spawn path 활성화 라운드에서 등록.
- **`_bossStatsSO` 부재 → X4-2에서 Inspector 재배치 필요**: trivial cost. SerializeField 한 줄 + 한 번의 prefab 재할당.
- **Buildup BossController FSM 참조 미반영**: 의도된 분할. X4-6에서 진행.

## Parallel Workstream

X3 runtime smoke test (user Play-mode host + 2P) — 파일 0겹침으로 독립 진행. X3-7.1 패치가 필요해도 본 라운드와 순서 충돌 없음.

## Spawned Follow-up

- **X4-2 NEXT**: 4 manager component attach (StatManager / StateManager / SkillExecutor / SkillManager `[RequireComponent]`) + `_bossStatsSO` SerializeField + Boss prefab 마이그레이션. PNC3D X3-2 패턴 미러.
