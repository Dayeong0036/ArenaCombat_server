# X1-6c-2: 3DScene Pool/Manager 3 GameObject 추가 — 2026-05-14

**Status**: APPLIED. Codex APPROVED WITH CHANGES via MCP. 코드 0.

## Scope

`Assets/Scenes/3DScene.unity` 1 파일 EDIT — line 4754 직전 (SceneRoots 블록 직전) + line 4776 m_Roots 끝 추가.

## 추가된 GameObject 3개

| GameObject | Transform | MonoBehaviour | Component | Settings |
|-----------|-----------|---------------|-----------|----------|
| `ProjectilePool` (9000001) | 9000002 | 9000003 | ProjectilePool | `_prefab → 7000006 + GUID 5ca17d1e… type 3` (SkillProjectile component), `_initialSize=10` |
| `PersistentAreaPool` (9100001) | 9100002 | 9100003 | PersistentAreaPool | `_prefab → 8000004 + GUID 1b8d63f8… type 3` (SkillArea component), `_initialSize=5` |
| `PersistentAreaManager` (9200001) | 9200002 | 9200003 | PersistentAreaManager | `_pool → 9100003` (PersistentAreaPool MonoBehaviour, scene-local) |

SceneRoots.m_Roots에 3 Transform fileID 추가: `9000002 / 9100002 / 9200002`.

## Codex Critical Applied

- **C-1 `_prefab` 참조 형식**: GameObject 아닌 **Component 참조**. SkillProjectile 컴포넌트 fileID 7000006 / SkillArea 컴포넌트 fileID 8000004 (X1-6c-1 prefab 작성 시 정해진 값). 외부 자산이므로 `type: 3` + 자산 GUID.
- **C-2 `_pool` scene-local Component 참조**: PersistentAreaPool **MonoBehaviour** fileID 9100003 (GameObject 9100001 아님). scene-local이므로 `{fileID: 9100003}` 만 (guid/type 없음).

## Codex Suggestions Applied

- fileID 범위 `9000001..9200003` 충돌 0 (Codex 검증)
- 삽입 위치 line 4754 직전 (SceneRoots 블록 시작 전)
- m_Roots는 Transform fileID (GameObject 아님), 마지막에 append
- Awake 부작용 안전:
  - ProjectilePool.Awake() singleton + prewarm 10
  - PersistentAreaPool.Awake() prewarm 5
  - PersistentAreaManager.Awake() singleton
  - 3 모두 DDOL 호출 없음 → scene-local

## Verification

1. **MCP get_gameobject ProjectilePool**:
   - `_prefab: "SkillProjectile"` ✅ (정상 참조)
   - `_initialSize: 10` ✅
   - components: Transform + ProjectilePool 표시
2. **콘솔 0 에러 / 0 경고** ✅
3. 씬 로드 클린 (load_scene 성공)

## Files

| Op | Path | LOC delta |
|---|---|---|
| EDIT | `Assets/Scenes/3DScene.unity` | +~135 (3 GameObject + 3 Transform + 3 MonoBehaviour 블록 + m_Roots 3 entries) |

## Risks (Acknowledged)

1. **NetworkPrefab 미등록**: pools가 Awake에서 prefab Instantiate 가능하지만 NetworkObject.Spawn() 호출 시 NGO 경고/실패. X1-6c-3에서 등록 후 해소.
2. **scene-local lifetime**: 현재 코드 상 DDOL 없음 → scene 전환 시 destroy. lobby 매니저 패턴과 충돌 가능성 있으면 후속 라운드에서 DDOL 추가 검토.
3. **fileID 9000001..9200003 향후 충돌**: 본 프로젝트에서 9자리 고정 prefix 패턴 (90/91/92) 후속 라운드에서 9300000+ 사용 권장.

## Spawned Follow-up

- **X1-6c-3 NEXT**: SampleScene NetworkManager.NetworkConfig.NetworkPrefabs에 SkillProjectile + SkillArea 2 prefab 등록. NGO Spawn() 활성화. **smoke verification 5 unblock의 마지막 단계**.

## Parallel Workstream

X3 / X4 라운드와 파일 0겹침 유지.
